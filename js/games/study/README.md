# Study

Browse the selected region pool, then open a country page with a locator map.

| File | Role |
|---|---|
| `info.js` | Profile: continent, subregion, neighbors. No DOM. |
| `view.js` | Search grid and country page |

The locator is `js/ui/country-map.js`, the same map Flag Master and Capital Quest use.
